name: Personregister testmiljö

on:
    push:
        branches:
            - 'main'
    pull_request:
        branches:
            - 'main'
    schedule:
        - cron: 0 0 8,10,12 12 *

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
              
            - name: test #TODO
              uses: actions/setup-python@v6
              with:
                python-version: '3.14'
            
            - name: Install dependencies
              run: |
                pip install -r requirements.txt
            
            - name: Run tests
              run: |
                python -m pytest tests/
    
    #TODO docker test